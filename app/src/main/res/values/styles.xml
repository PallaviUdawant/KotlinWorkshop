<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_affected_countries</item>
        <item name="colorPrimaryDark">@color/color_affected_countries</item>
        <item name="colorAccent">@color/color_affected_countries</item>
    </style>

</resources>
